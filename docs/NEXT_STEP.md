# 下一步

## 当前阶段

- 下一闭环进入阶段 4：运行态读取改造。
- 目标不是扩功能，而是把状态页和审计结果从“本地配置视角”收口到“运行态优先”。

## 下一最小闭环

- 让 `mihomo status` 的“当前模式”优先读取 Mihomo REST API 运行态
- 控制面不可达时保留本地配置回退，不影响现有可用性
- 在输出中明确区分“运行态读取结果”与“本地配置值”
- 补 focused tests，覆盖 REST 成功与回退场景

## 本轮不做

- 不扩 `external-controller-tls` 实现
- 不新增用户可感知功能
- 不调整订阅真相边界
- 不做大规模脚本拆分

## 退出条件

- `mihomo status` 可在控制面可达时读到运行态模式
- 控制面不可达时继续稳定回退
- 相关 smoke / service-mock 回归通过
- 文档同步更新当前阶段结论
