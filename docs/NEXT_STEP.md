# 下一步

## 当前阶段

- 下一闭环进入阶段 5：代码结构收口。
- 当前已完成第六刀：`status` 的推荐下一步判断已抽到共用 helper。
- 目标不是扩功能，而是在不改变行为的前提下，继续消除展示层重复逻辑。

## 下一最小闭环

- 提取 `status` 的警告与收尾展示 helper
- 保持现有输出文本、顺序与退化行为不变
- 补 focused tests，确保重构不改变行为
- 文档同步切到阶段 5 当前真相

## 本轮不做

- 不新增用户可感知功能
- 不调整运行态真相边界
- 不扩 `external-controller-tls` 实现
- 不做大规模脚本拆分

## 退出条件

- `status` 的警告与收尾展示逻辑不再以内联形式存在
- 现有 `status` 输出文本、顺序与警告行为保持不变
- 相关 smoke / service-mock 回归通过
- 文档同步更新当前阶段结论
