# 下一步

## 当前阶段

- 下一闭环进入阶段 4：运行态读取改造。
- 当前已完成第四刀：`mihomo status` 与 `mihomo runtime-audit` 已能输出最小策略组运行态摘要。
- 目标不是扩功能，而是把状态页和审计结果从“本地配置视角”继续收口到“运行态优先”。

## 下一最小闭环

- 让 `mihomo status` 或 `mihomo runtime-audit` 输出最小控制面运行态摘要
- 继续复用当前的控制面读取助手，避免重复实现
- 控制面不可达时保留本地配置回退，不影响状态页与审计输出可用性
- 补 focused tests，覆盖 REST 成功与回退场景

## 本轮不做

- 不扩 `external-controller-tls` 实现
- 不新增用户可感知功能
- 不调整订阅真相边界
- 不做大规模脚本拆分

## 退出条件

- 至少一个用户入口能在控制面可达时读到最小控制面运行态摘要
- 控制面不可达时继续稳定回退
- 相关 smoke / service-mock 回归通过
- 文档同步更新当前阶段结论
